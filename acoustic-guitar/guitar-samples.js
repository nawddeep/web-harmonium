// acoustic-guitar/guitar-samples.js

/**
 * Mapping of MIDI note numbers to sample URLs.
 * Populate this mapping with your actual guitar sample paths.
 * If a note doesn't exist here, the GuitarEngine will seamlessly 
 * fallback to the Karplus-Strong synthesis algorithm.
 */
const guitarSampleMap = {
    // Example:
    // 60: 'samples/acoustic-guitar/C4.wav',
    // 64: 'samples/acoustic-guitar/E4.wav'
};
